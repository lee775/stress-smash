{
  "name": "stressgame",
  "version": "1.0.0",
  "private": true,
  "description": "스트레스 박살 - 교수님 시리즈 (PC 웹 + 안드로이드 PWA/APK)",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "cap:add": "cap add android",
    "cap:sync": "npm run build:web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
